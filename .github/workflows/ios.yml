name: iOS

on:
  push:
    branches:
      - main
    paths:
      - "ios/**"
      - "release/version.json"
      - "scripts/release-version.mjs"
      - "scripts/package-ios-device-ipa.sh"
      - ".github/workflows/ios.yml"
  pull_request:
    paths:
      - "ios/**"
      - "release/version.json"
      - "scripts/release-version.mjs"
      - "scripts/package-ios-device-ipa.sh"
      - ".github/workflows/ios.yml"
  workflow_call:
    inputs:
      release_candidate:
        required: false
        type: boolean
        default: false
      version:
        required: false
        type: string
        default: ""
      build_number:
        required: false
        type: string
        default: ""
      version_override:
        description: Apply version and build inputs inside the runner without changing repository source.
        required: false
        type: boolean
        default: false
      source_ref:
        required: false
        type: string
        default: ""
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    if: ${{ inputs.release_candidate || github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release/') }}
    runs-on: macos-26

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.source_ref || github.sha }}
          persist-credentials: false

      - name: Apply direct candidate version
        if: ${{ inputs.release_candidate && inputs.version_override }}
        shell: bash
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
          EXPECTED_BUILD: ${{ inputs.build_number }}
        run: node scripts/release-version.mjs --set "$EXPECTED_VERSION" "$EXPECTED_BUILD"

      - name: Verify candidate version
        if: ${{ inputs.release_candidate }}
        shell: bash
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
          EXPECTED_BUILD: ${{ inputs.build_number }}
        run: |
          node scripts/release-version.mjs --check
          read -r VERSION BUILD < <(node -e 'const value = require("./release/version.json"); console.log(value.version, value.build)')
          [[ "$VERSION" == "$EXPECTED_VERSION" ]]
          [[ "$BUILD" == "$EXPECTED_BUILD" ]]

      - name: Verify Xcode toolchain
        shell: bash
        run: |
          xcodebuild -version
          swift --version
          XCODE_MAJOR="$(xcodebuild -version | awk 'NR == 1 { split($2, version, "."); print version[1] }')"
          (( XCODE_MAJOR >= 26 ))

      - name: Restore iOS DerivedData cache
        id: ios-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ runner.temp }}/ResonanceDerivedData
          key: resonance-ios-derived-data-${{ runner.arch }}-${{ hashFiles('ios/Resonance.xcodeproj/project.pbxproj', 'ios/Resonance/**') }}
          restore-keys: |
            resonance-ios-derived-data-${{ runner.arch }}-

      - name: Build for iOS Simulator
        run: |
          xcodebuild \
            -project ios/Resonance.xcodeproj \
            -scheme Resonance \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath "$RUNNER_TEMP/ResonanceDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Verify built iOS version
        if: ${{ inputs.release_candidate }}
        shell: bash
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
          EXPECTED_BUILD: ${{ inputs.build_number }}
        run: |
          PLIST="$RUNNER_TEMP/ResonanceDerivedData/Build/Products/Debug-iphonesimulator/Resonance.app/Info.plist"
          [[ "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$PLIST")" == "$EXPECTED_VERSION" ]]
          [[ "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "$PLIST")" == "$EXPECTED_BUILD" ]]

      - name: Build unsigned arm64 device app
        if: ${{ inputs.release_candidate }}
        shell: bash
        run: |
          xcodebuild \
            -project ios/Resonance.xcodeproj \
            -scheme Resonance \
            -configuration Release \
            -sdk iphoneos \
            -destination generic/platform=iOS \
            -derivedDataPath "$RUNNER_TEMP/ResonanceDeviceDerivedData" \
            ARCHS=arm64 \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Verify built device version
        if: ${{ inputs.release_candidate }}
        shell: bash
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
          EXPECTED_BUILD: ${{ inputs.build_number }}
        run: |
          PLIST="$RUNNER_TEMP/ResonanceDeviceDerivedData/Build/Products/Release-iphoneos/Resonance.app/Info.plist"
          [[ "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$PLIST")" == "$EXPECTED_VERSION" ]]
          [[ "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "$PLIST")" == "$EXPECTED_BUILD" ]]

      - name: Upload simulator app
        if: ${{ !inputs.release_candidate }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: Resonance-iOS-Simulator-${{ github.sha }}
          path: ${{ runner.temp }}/ResonanceDerivedData/Build/Products/Debug-iphonesimulator/Resonance.app
          if-no-files-found: error

      - name: Package iOS Simulator release candidate
        if: ${{ inputs.release_candidate }}
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
        run: |
          mkdir -p installers/ios/dist
          ditto -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/ResonanceDerivedData/Build/Products/Debug-iphonesimulator/Resonance.app" \
            "installers/ios/dist/Resonance-iOS-Simulator-${VERSION}.zip"
          cd installers/ios/dist
          shasum -a 256 "Resonance-iOS-Simulator-${VERSION}.zip" > "Resonance-iOS-Simulator-${VERSION}.zip.sha256"

      - name: Package iOS device release candidate
        if: ${{ inputs.release_candidate }}
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
        run: |
          scripts/package-ios-device-ipa.sh \
            "$RUNNER_TEMP/ResonanceDeviceDerivedData/Build/Products/Release-iphoneos/Resonance.app" \
            "installers/ios/dist/Resonance-iOS-Device-${VERSION}.ipa"

      - name: Upload iOS release candidate
        if: ${{ inputs.release_candidate }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-assets-ios
          path: |
            installers/ios/dist/Resonance-iOS-Simulator-${{ inputs.version }}.zip
            installers/ios/dist/Resonance-iOS-Simulator-${{ inputs.version }}.zip.sha256
            installers/ios/dist/Resonance-iOS-Device-${{ inputs.version }}.ipa
            installers/ios/dist/Resonance-iOS-Device-${{ inputs.version }}.ipa.sha256
          if-no-files-found: error
          retention-days: 14
          overwrite: true

      - name: Save iOS DerivedData cache from main
        if: ${{ github.ref == 'refs/heads/main' && steps.ios-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ runner.temp }}/ResonanceDerivedData
          key: ${{ steps.ios-cache.outputs.cache-primary-key }}
